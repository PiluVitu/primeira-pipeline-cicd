name: CI/CD

on:
  push:
    branches:
      - master
      - pipe-ci
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  CI:
    runs-on: ubuntu-22.04
    steps:
      - name: Pegando código do repo
        uses: actions/checkout@v4

      - name: LogIn Docker hub
        uses: docker/login-action@v3
        with:
          username: ${{secrets.DOCKERHUB_USERNAME}}
          password: ${{secrets.DOCKERHUB_TOKEN}}

      - name: Build and push Docker images
        uses: docker/build-push-action@v6.5.0
        with:
          context: ./src
          push: true
          tags: |
            piluvitu/aula-primeira-pipiline:v${{github.run_number}}
            piluvitu/aula-primeira-pipiline:latest
